{
  "name": "va-client",
  "version": "0.0.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "lint": "eslint src",
    "prettify": "yarn prettier src --check",
    "prettier:fix": "yarn prettier -- --write",
    "test": "mocha --require ts-node/register test/**/*.ts"
  },
  "dependencies": {
    "@solana/web3.js": "workspace:*",
    "@types/node": "^22.9.2"
  }
}
